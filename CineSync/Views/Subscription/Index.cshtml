@model IEnumerable<CineSync.Models.Subscription>
@{
    ViewData["Title"] = "Subscriptions";
    var searchResults = ViewBag.SearchResults as List<(CineSync.Models.Subscription Subscription, double Score)>;
}

<div class="cs-max-1400">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Available Subscriptions</h2>
        <div class="d-flex gap-2">
            @if (User.IsInRole("Admin"))
            {
                <a asp-action="Manage" class="btn cs-btn cs-btn-dislike">Manage</a>
            }
            @if (User.Identity?.IsAuthenticated == true)
            {
                <a asp-action="Cart" class="btn cs-btn cs-btn-neutral">My Cart</a>
            }
        </div>
    </div>

    <form asp-action="Index" method="get" class="mb-4">
        <div class="row g-3 align-items-center cs-panel">
            <div class="col-md-5">
                <div class="input-group">
                    <span class="input-group-text">Search</span>
                    <input type="text" name="search" value="@ViewBag.Search"
                           class="form-control" placeholder="Search by platform, type, quality..." />
                </div>
            </div>
            <div class="col-md-3">
                <select name="platformId" class="form-select">
                    <option value="">All Platforms</option>
                    @foreach (var p in (IEnumerable<CineSync.Models.Platform>)ViewBag.Platforms)
                    {
                        <option value="@p.PlatformId"
                                selected="@(ViewBag.SelectedPlatform == p.PlatformId ? "selected" : null)">
                            @p.Name
                        </option>
                    }
                </select>
            </div>
            <div class="col-md-2">
                <select name="sortBy" class="form-select">
                    <option value="">Default order</option>
                    <option value="price_asc" selected="@(ViewBag.SortBy == "price_asc" ? "selected" : null)">
                        Price ↑
                    </option>
                    <option value="price_desc" selected="@(ViewBag.SortBy == "price_desc" ? "selected" : null)">
                        Price ↓
                    </option>
                </select>
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn cs-btn cs-btn-blue w-100">Search</button>
            </div>
        </div>
        @if (!string.IsNullOrEmpty(ViewBag.Search as string))
        {
            <div class="mt-2">
                <a asp-action="Index" class="btn cs-btn cs-btn-neutral">Clear</a>
                <span class="cs-muted ms-2 small">
                    Results for "<strong>@ViewBag.Search</strong>"
                    — @Model.Count() found
                </span>
            </div>
        }
    </form>

    @if (!Model.Any())
    {
        <div class="alert alert-info">No subscriptions found.</div>
    }
    else
    {
        @foreach (var group in Model.GroupBy(s => s.Platform?.Name))
        {
            <h4 class="mt-4 mb-3">@group.Key</h4>
            <div class="cs-grid-movies">
                @foreach (var sub in group)
                {
                    double? score = searchResults?.FirstOrDefault(r => r.Subscription.SubscriptionId == sub.SubscriptionId).Score;

                    <div>
                        <div class="cs-card movie-card p-4 h-100">
                                <h5 class="card-title">@sub.Type</h5>
                                <p class="fw-bold fs-5 mb-1">@sub.MonthlyPrice RON/mo</p>
                                <p class="cs-muted small mb-2">Quality: @sub.VideoQuality</p>
                                @if (score.HasValue)
                                {
                                    <span class="cs-badge cs-badge-watched">
                                        Relevance @score.Value.ToString("F3")
                                    </span>
                                }
                            <div class="d-flex flex-column gap-2 pt-4">
                                <a asp-action="Details" asp-route-id="@sub.SubscriptionId"
                                   class="btn cs-btn cs-btn-blue w-100">View Details</a>
                                @if (User.Identity?.IsAuthenticated == true)
                                {
                                    <form asp-action="AddToCart" method="post">
                                        <input type="hidden" name="subscriptionId" value="@sub.SubscriptionId" />
                                        <button type="submit" class="btn cs-btn cs-btn-watchlist w-100">
                                            Add to Cart
                                        </button>
                                    </form>
                                }
                                else
                                {
                                    <a asp-area="Identity" asp-page="/Account/Login"
                                       class="btn cs-btn cs-btn-neutral w-100">Login to subscribe</a>
                                }
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
    }
</div>
